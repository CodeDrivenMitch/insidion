%nav{class: 'navbar navbar-default'}
  %div.container-fluid
    %div.navbar-header
      %button{type: 'button', class: 'navbar-toggle collapsed', data: {toggle: 'collapse', target: '#main_menu'}}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{href: '#'} Insidion
    %div#main_menu.collapse.navbar-collapse
      %ul.nav.navbar-nav.navbar-right
        - if user_signed_in?
          = "Welcome back, #{current_user.email}"
          %li= link_to 'Logout', :destroy_user_session, :method => :delete
        - else
          %li= link_to 'Login', :new_user_session
          %li= link_to 'Register', :new_user_registration
